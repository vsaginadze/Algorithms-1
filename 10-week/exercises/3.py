def valid_heap(arr):
    pass

# check if array is valid binary heap
# what is valid binary heap?

    # is complete
    # for every node except the very last the left node exists and right node exists

    # left node and right node are less than parent
    # parent = i
    # left -> 2i + 1
    # right -> 2i + 2
    # 
